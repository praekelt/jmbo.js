=Jmbo.js=

Jmbo.js is a set of components built on top of Backbone.js. Jmbo.js' 
intention is to provide simple decoupled components that are most commonly
found in iOS and Android app development.

==StackView==

A hierachical navigation view which allows you to push and pop Backbone
Views. Views are transitioned with CSS animations.

==TabView==

Allows you to switch between views.

